<h2>ユーザー一覧</h2>

<table>
<tr>
  <th>ユーザー名</th>
  <th>ユーザー詳細ページ</th>
</tr>
<% @users.each do |user| %>
<tr>
  <td><%= user.username %></td>
  <td><%= link_to user.email, user_requests_path(user) %></td>
</tr>
<% end %>
</table>
